# TODO

## Completed
- Updated `frontend/src/components/Navbar.jsx` to use `frontend/src/assets/Logo.png` instead of “Karevo” text.
- Fixed `frontend/src/components/Hero.jsx` after accidental JSX breakage.
- Rewrote `frontend/src/components/Features.jsx` to replace brand color `#25789e` with `#3B00C5`.

## Next
- Replace remaining occurrences of `#25789e` with `#3B00C5` across the codebase.
  - Expected files from search: `components/Footer.jsx`, `components/CookiePolicy.jsx`, `components/CTASection.jsx`, `components/WaitlistModal.jsx`, `pages/Contact.jsx`, `pages/SignUp.jsx`, `components/KarevoID.jsx`, and others.
- After replacements, run `frontend` build to verify no TS/JSX errors.

